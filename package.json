{
  "dependencies": {
    "playwright": "^1.40.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.1",
    "allure-playwright": "^2.9.2"
  },
  "scripts": {
    "runSuite" : "npx playwright test tests/Sellia --workers 1 --project chromium --headed && npm run openReport",
    "openReport" : "npx playwright show-report reports/html-report"
  }
}
